# Docs site (this repo) — Next.js 16 + Fumadocs
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Edge redirect on the docs deploy itself
[[redirects]]
  from = "/docs"
  to = "/docs/introduction/what-is-kodeus"
  status = 307
